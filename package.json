{
  "name": "@polly-os/core",
  "version": "1.0.0",
  "scripts": {
    "deploy-rinkeby": "npx hardhat run --network rinkeby scripts/deploy-rinkeby.js ",
    "deploy-local": "npx hardhat run --network localhost scripts/deploy-local.js ",
    "deploy-mainnet": "npx hardhat run --network mainnet scripts/deploy-mainnet.js ",
    "test": "npx hardhat test",
    "dev": "npx nodemon -e sol,js --exec \"npm run test\""
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.3",
    "@nomiclabs/hardhat-etherscan": "^2.1.6",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "chai": "^4.3.4",
    "dotenv": "^10.0.0",
    "ethereum-waffle": "^3.4.0",
    "ethers": "^5.5.2",
    "hardhat": "^2.8.0",
    "hardhat-abi-exporter": "^2.3.0",
    "hardhat-gas-reporter": "^1.0.4"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.4.1",
    "colors": "^1.4.0"
  }
}
